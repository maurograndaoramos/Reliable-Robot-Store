# Currently not in use as I am using git commands directly for testing purposes, but this should contain the commands that would help the developer automate the majority of required processes for CI/CD.

dev:
	git checkout -b dev

master:
	git checkout master